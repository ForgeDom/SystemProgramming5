<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,5">
            <TextBox x:Name="sourceTextBox" Width="400" Margin="0,0,5,0"/>
            <Button Content="Вибрати джерело" Click="BrowseSource_Click"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,5">
            <TextBox x:Name="destinationTextBox" Width="400" Margin="0,0,5,0"/>
            <Button Content="Вибрати ціль" Click="BrowseDestination_Click"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,10">
            <Label Content="Кількість потоків:" VerticalAlignment="Center"/>
            <ComboBox x:Name="threadCountComboBox" Width="60" Margin="10,0,0,0">
                <ComboBoxItem Content="1" IsSelected="True"/>
                <ComboBoxItem Content="2"/>
                <ComboBoxItem Content="4"/>
                <ComboBoxItem Content="8"/>
            </ComboBox>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,10">
            <ProgressBar x:Name="progressBar" Height="20"/>
            <TextBlock x:Name="progressText" TextAlignment="Center" Margin="0,5"/>
        </StackPanel>

        <Button Content="Старт" Grid.Row="4" Height="30" Click="Start_Click" HorizontalAlignment="Center" Width="100"/>
    </Grid>
</Window>
