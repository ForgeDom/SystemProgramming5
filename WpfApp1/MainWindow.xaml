<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="MainWindow" Height="250" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="inputTextBox" Height="100" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

        <Button Grid.Row="1" Content="Аналізувати" Height="30" Width="100" Margin="0,10,0,0" Click="AnalyzeButton_Click"/>

        <ProgressBar Grid.Row="2" x:Name="progressBar" Height="20" Margin="0,10,0,0" IsIndeterminate="True" Visibility="Collapsed"/>

        <StackPanel Grid.Row="3" Margin="0,10,0,0">
            <TextBlock x:Name="vowelsTextBlock" Text="Голосні: " Margin="0,0,0,5"/>
            <TextBlock x:Name="consonantsTextBlock" Text="Приголосні: " Margin="0,0,0,5"/>
            <TextBlock x:Name="symbolsTextBlock" Text="Символи: " Margin="0,0,0,5"/>
        </StackPanel>
    </Grid>
</Window>