<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="MainWindow" Height="250" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Введіть число:" VerticalAlignment="Center"/>
            <TextBox x:Name="numberTextBox" Width="200" Margin="10,0,0,0"/>
        </StackPanel>

        <Button Grid.Row="1" Content="Обчислити факторіал" Click="CalculateButton_Click" Height="30" Width="200" HorizontalAlignment="Left"/>

        <TextBlock Grid.Row="2" x:Name="resultTextBlock" Text="Результат: " Margin="0,10,0,0" TextWrapping="Wrap"/>
        <ProgressBar Grid.Row="3" x:Name="progressBar" Height="20" IsIndeterminate="True" Visibility="Collapsed" Margin="0,10,0,0"/>
    </Grid>
</Window>