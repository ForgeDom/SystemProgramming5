<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
     <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Вибір вихідного файлу -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,5">
            <TextBox x:Name="sourcePathTextBox" Width="400" Margin="0,0,5,0"/>
            <Button Content="Вибрати файл" Click="BrowseSource_Click" Width="100"/>
        </StackPanel>

        <!-- Вибір цільової папки -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5">
            <TextBox x:Name="destinationPathTextBox" Width="400" Margin="0,0,5,0"/>
            <Button Content="Вибрати папку" Click="BrowseDestination_Click" Width="100"/>
        </StackPanel>

        <!-- Кількість потоків -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10">
            <Label Content="Кількість потоків:" VerticalAlignment="Center"/>
            <ComboBox x:Name="threadCountComboBox" Width="100" Margin="10,0,0,0">
                <ComboBoxItem Content="1" IsSelected="True"/>
                <ComboBoxItem Content="2"/>
                <ComboBoxItem Content="4"/>
                <ComboBoxItem Content="8"/>
            </ComboBox>
        </StackPanel>

        <!-- Прогрес -->
        <StackPanel Grid.Row="3" Margin="0,10">
            <ProgressBar x:Name="progressBar" Height="20" Minimum="0" Maximum="100"/>
            <TextBlock x:Name="progressText" TextAlignment="Center" Margin="0,5"/>
            <TextBlock x:Name="speedText" TextAlignment="Center" FontStyle="Italic"/>
        </StackPanel>

        <!-- Логування -->
        <TextBox x:Name="logTextBox" Grid.Row="4" Margin="0,10"
                 IsReadOnly="True" VerticalScrollBarVisibility="Auto"
                 TextWrapping="Wrap" AcceptsReturn="True"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
            <Button x:Name="startButton" Content="Старт" Width="100" Height="30" Click="StartButton_Click"/>
            <Button x:Name="pauseButton" Content="Пауза" Width="100" Height="30" Margin="10,0,0,0"
                    Click="PauseButton_Click" IsEnabled="False"/>
            <Button x:Name="cancelButton" Content="Скасувати" Width="100" Height="30" Margin="10,0,0,0"
                    Click="CancelButton_Click" IsEnabled="False"/>
        </StackPanel>
    </Grid>
</Window>
